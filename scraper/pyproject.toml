[project]
name = "scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Matko Manzin", email = "matko@mononetz.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "playwright>=1.62.0",
]

[project.scripts]
scraper = "scraper:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
